# Database Configuration
db.url=jdbc:mysql://localhost:3306/dangerzone_ormoc?useSSL=false&serverTimezone=UTC
db.username=root
db.password=
db.driver=com.mysql.cj.jdbc.Driver

# Connection Pool
db.pool.maxConnections=10
db.pool.minConnections=2

# Web Server Configuration
server.port=8080
server.host=127.0.0.1

# Application Settings
app.name=Ormoc Danger Zone Mapping System
app.version=1.0.0